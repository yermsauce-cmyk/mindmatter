Place your high-resolution background artwork here as `assets/bg.jpg`.
Recommended: 1600px wide, optimized JPG/WEBP. The site uses this as the page background.

Optional merch images: add small thumbnails into `assets/merch/` and update the HTML markup.
